[project]
name = "hivebox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mateus", email = "---" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "httpx>=0.28.1",
]

[project.scripts]
devops-hands-on-project-hivebox = "devops_hands_on_project_hivebox:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
